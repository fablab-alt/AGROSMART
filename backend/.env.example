# Configuration Backend - AgriSmart CI
# ==========================================
# Copier ce fichier en .env et remplacer les <PLACEHOLDER>
# Ne JAMAIS commiter le fichier .env
# ==========================================

NODE_ENV=development

# ==========================================
# DATABASE - MySQL + Prisma
# ==========================================
# Générer mot de passe: openssl rand -base64 24
DATABASE_URL="mysql://agrismart:<MOT_DE_PASSE>@localhost:3306/agrismart_ci"
DB_HOST=localhost
DB_PORT=3306
DB_NAME=agrismart_ci
DB_USER=agrismart
DB_PASSWORD=<GÉNÉRER_MOT_DE_PASSE>
MYSQL_ROOT_PASSWORD=<GÉNÉRER_MOT_DE_PASSE_ROOT>
MYSQL_PASSWORD=<GÉNÉRER_MOT_DE_PASSE>

# ==========================================
# JWT
# ==========================================
# Générer: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=<GÉNÉRER_SECRET_JWT>
JWT_EXPIRES_IN=7d
JWT_REFRESH_EXPIRES_IN=30d

# ==========================================
# OTP
# ==========================================
OTP_EXPIRES_MINUTES=10
OTP_MAX_ATTEMPTS=3

# ==========================================
# TWILIO (Optionnel)
# ==========================================
# TWILIO_ACCOUNT_SID=
# TWILIO_AUTH_TOKEN=
# TWILIO_PHONE_NUMBER=

# ==========================================
# EMAIL (Optionnel)
# ==========================================
# SMTP_HOST=
# SMTP_PORT=
# SMTP_USER=
# SMTP_PASSWORD=

# ==========================================
# CORS
# ==========================================
ALLOWED_ORIGINS=http://localhost:3601,http://localhost:3603
CORS_ORIGIN=http://localhost:3601,http://localhost:3603
ALLOW_LOCALHOST_CORS=true

# ==========================================
# RATE LIMITING
# ==========================================
RATE_LIMIT_WINDOW_MS=60000
RATE_LIMIT_MAX_REQUESTS=5000
